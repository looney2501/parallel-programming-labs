#include "../utils/utils-lab3.cpp"
#include <algorithm>

using std::min;

int main(int argc, char** argv) {
    int *v1, *v2, n1, n2, minL;

    readVectorFromFile(v1, n1, R"(..\lab3\resources\input\number1.txt)");
    readVectorFromFile(v2, n2, R"(..\lab3\resources\input\number2.txt)");
    minL = min(n1, n2);
    for (int i = minL)

    //secvential

}